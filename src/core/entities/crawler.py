import subprocess


class Crawler:
    def __init__(self):
        self.urls = []

    # self.shell = True
    # self.executable = "/bin/bash"
    # self.stdout = subprocess.PIPE
    # self.stderr = subprocess.PIPE
    # self.text = True

